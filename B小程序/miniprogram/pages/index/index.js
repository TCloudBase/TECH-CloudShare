const app = getApp()
Page({
	async onLoad(){
		// const r1 = await (await app.cloud()).callFunction({name:'test'})
		// console.log(r1)
		// const r2 = await (await app.cloud()).database().collection('user').doc('123').get()
    	// console.log(r2)
	}
})
